# Firebase Data Connect Connector Configuration

connectorId: nakevlink-connector

# SDK generation configuration
generate:
  javascriptSdk:
    outputDir: "../../web-app/src/lib/dataconnect"
    package: "@nakevlink/dataconnect"